import React, { Component } from "react";

class Count extends Component {
    constructor(props){
        super(props);

        this.state = {
            // So where is this going to get the todos from?
        }
    }
}